import { BasePage } from './BasePage';

/**
 * A minimal page object that inherits navigation from BasePage.
 * Add reusable accessibility-centric helpers here (e.g., landmark checks).
 */
export class GenericPage extends BasePage {
  // space for reusable a11y helpers later (landmark checks, skip links, etc.)
}
